import weatherIcon from 'weather-app/utils/weather-icon';
import { module, test } from 'qunit';

module('Unit | Utility | weather-icon', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = weatherIcon();
    assert.ok(result);
  });
});
